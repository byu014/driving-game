const directions = {
  ArrowUp: 'north',
  ArrowDown: 'south',
  ArrowRight: 'east',
  ArrowLeft: 'west'
};
const car = {
  direction: 'east'
};

const $img = document.querySelector('img');

window.addEventListener('keydown', function (event) {
});
